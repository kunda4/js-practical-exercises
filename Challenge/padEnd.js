// Question : find the code output with (reason)

let str1 = 'Learn';
let str2 = 'JavaScript';

console.log(str1.padEnd(6) + str2.padEnd(1))

// the output will be the "Learn JavaScript"
/*it will print str1 which 5 length and add space 
to fill 6 then concate with str2 and because we 
have less number of than the length of str it will 
printed all.*/